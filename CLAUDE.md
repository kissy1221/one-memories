## ルール

改修したら必ずテストをすること。

機能追加した場合も、内容に対するテストコードを実装すること

テストを通すまでは、pushはNG

機能追加・改修・タスク単位でfeature/xxxのブランチを切って作業をすること

コミットメッセージは作業内容がわかりやすく日本語ですること

作業後にはPRをするため、背景や実装内容など出力すること

まとまりがよい状態かつテストが通っている状態で、都度コミットをすること。
